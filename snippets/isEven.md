---
title:  isEven
tags: math,beginner
---
Returns `true` if the given number is even, `false` otherwise.

```php
function isEven($number)
{
  return ($number % 2) === 0;
}
```

```php
isEven(4); // true
```
